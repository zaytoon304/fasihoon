# فصحون — تطبيق مستقل لتعلّم اللغة العربية

مأخوذ من مشروع فكّر بتاريخ 2026-09-08 ليصير تطبيقاً مستقلاً بذاته، بدون حذف أي شيء من نسخة فكّر الأصلية.

## خطوات البناء
- [x] إنشاء المجلد + نسخ الملفات ذات الصلة من فكّر: `games/fasihoon.html`، `games/asas.html`، وكل ملفات `shared/` المطلوبة (style.css، firebase-config.js، economy-config.js، economy.js، asas-data*.js)، و`store.html`، `teacher.html`
- [x] `shared/progress.js` أُعيد كتابته: `FAKKER_GAMES` صار نشاطين فقط (فصحون + أساس القراءة)، أُزيل `Fakker.Daily` و`Fakker.CheckIns` بالكامل (غير مستخدمين بلا بوابة حصة انتظار)
- [x] أُزيلت كل استدعاءات `Fakker.CheckIns.isCheckedIn()` من `games/fasihoon.html`، `games/asas.html`، و`store.html` (كانت تكسر الصفحات بصمت — اكتُشف بالاختبار الفعلي بالمتصفح)
- [x] `teacher.html`: أُزيل تبويب "سجل حصص الانتظار" بالكامل (تبويب + دالة renderCheckins + استخدامات Fakker.CheckIns بالإحصائيات)
- [x] إعادة تسمية العلامة التجارية من "فكّر 🧠" إلى "فصحون 📖" بكل الملفات (العناوين، الشريط العلوي)
- [x] `index.html` جديد كلياً: شاشة ترحيب (اسم + شخصية، بلا بوابة حصة انتظار) → صفحة رئيسية ببطاقتين كبيرتين (📖 فصحون يفتح شاشة اختيار المستوى الدراسي الخمسة، 🌱 أساس القراءة يفتح مباشرة لأنها تدير مراحلها بنفسها)
- [x] اختُبر بالمتصفح فعلياً بالكامل: الترحيب، الصفحة الرئيسية، اختيار المستوى، لعب فصحون، أساس القراءة (٢٥ مرحلة)، المتجر، لوحة المعلم (فتح بالـ PIN، تبويب الإحصائيات يعرض النشاطين الصحيحين) — صفر أخطاء كونسول بعد إصلاح مراجع CheckIns المتبقية ✅
- [x] تهيئة git + رفع على GitHub — مستودع عام منفصل: `github.com/zaytoon304/fasihoon`، منشور: **https://zaytoon304.github.io/fasihoon/**

## ملاحظة
نسخة فكّر الأصلية (`../fakker/games/fasihoon.html` و`../fakker/games/asas.html`) لم تُمس إطلاقاً — هذا مشروع مستقل بمجلد وشيفرة خاصة به بالكامل.

## تحديثات ما بعد الإطلاق (نفس اليوم 2026-09-09)

**١. فتح كل المراحل دفعة وحدة (للمعلم):** `games/asas.html?unlock=all` يفتح كل مراحل أساس القراءة بـ٣ نجوم فوراً على أي جهاز يُفتح فيه — يفيد وقت الحصة بدل انتظار التقدم التسلسلي الطبيعي.

**٢. مساحة إجابة مكتوبة بكل أسئلة قِطع القراءة** (بدل نقاش شفهي فقط) — تُحفظ محلياً تلقائياً، وأيضاً على Firebase لو مفعّل.

**٣. مرحلتان جديدتان من قِطع الاستيعاب المرسلة من محمد** (٢٦: ٣٠ قطعة، ٢٧: ١٥ قطعة أطول وأكثر ثراءً بالأحداث) — كل قطعة: قصة + أسئلة فهم + إملاء + استخرج + أكمل القصة + خاتمة.

**٤. تتبع إجابات الطلاب للمعلم:** `Fakker.Answers` يحفظ كل إجابة على Firebase (اسم الطالب + النص)، وتبويب جديد بلوحة المعلم **"✍️ إجابات الطلاب"** يسرد كل طالب وعدد إجاباته، وبالضغط عليه تظهر كل إجاباته مجمّعة حسب المقطع مع نص السؤال. **يحتاج مشروع Firebase حقيقي مفعّل ليعمل عبر الأجهزة** (بدونه، كل جهاز يحفظ بياناته محلياً فقط ولوحة المعلم تعرض تنبيهاً واضحاً بدل خطأ) — بانتظار محمد يرسل قيم Firebase config.

**٥. الكلمات عالية التكرار (Sight Words) — ١٠ مراحل جديدة (٢٨-٣٧):** ضمائر، كلمات مكان، كلمات زمن، كلمات ربط، أدوات سؤال، أفعال متكررة (مرحلتان)، أسماء قصص متكررة (مرحلتان)، صفات متكررة — كلمات يجب أن تُقرأ مباشرة بلا تهجئة، مبنية من قائمة تربوية فصّلها محمد بالكامل.

**٦. قِطع متدرجة حقيقياً — ٥ مراحل جديدة (٣٨-٤٢)، ١٥ قطعة أصلية من تأليف جديد:** المستوى ١ (جمل ٤-٦ كلمات فقط، بلا إملاء/استخراج) ← المستوى ٢ (ربط بـثم/بعد/عند/لأن) ← المستوى ٣ (سبب ونتيجة + حوار بسيط) ← المستوى ٤ (قصة ٦-٨ جمل + استنتاج) ← المستوى ٥ (سرد كامل + ترتيب أحداث + استنتاج + خاتمة). الكلمات الذهبية الخمسون تتكرر عمداً عبر القِطع لتثبيت القراءة الفورية.

**النطاق الكامل الآن: ٤٢ مرحلة بأساس القراءة.** كل التحديثات اختُبرت بالمتصفح فعلياً (إكمال كامل لكل مرحلة جديدة بلا أخطاء كونسول) قبل الرفع. لا عمل ناقص معروف غير خطوة Firebase (بانتظار محمد).

## تحديث 2026-09-10: أسئلة الفهم صارت اختيار من متعدد

بناءً على طلب محمد: "بدلاً من أن يكتب الطالب، تعطيه اختيار الإجابة من متعدد" — كل أسئلة الفهم (comprehension) بجميع مراحل قِطع القراءة (٢٤، ٢٥، ٢٦، ٢٧، ٣٨-٤٢ = ٩ مراحل، ٤٤٨ سؤال إجمالاً) تحولت من صندوق كتابة حر إلى أزرار اختيار من متعدد حقيقية.

**كيف تعمل تقنياً:**
- كل سؤال بملفات `asas-data-phase4/5/6/7.js` صار له `answers: [...]` — مصفوفة موازية لـ`comprehension`/`questions`، فيها الإجابة الصحيحة النصية لكل سؤال بنفس الترتيب.
- محرك العرض (`games/asas.html`) يبني ٤ اختيارات لكل سؤال: الإجابة الصحيحة + ٣ مشتتات تُسحب عشوائياً من إجابات باقي مقاطع نفس المرحلة (لا تكرار، ولا تُستخدم إجابة السؤال نفسه كمشتت).
- عند اختيار الطالب: كل الأزرار تُقفل فوراً (`locked`)، الإجابة الصحيحة تلوّن أخضر دائماً، واختيار الطالب الخاطئ (إن وُجد) يلوّن أحمر. يُحفظ الاختيار في localStorage وعلى Firebase (لو مفعّل) بنص مثل: `"✅ صحيح — اختار: ..."` أو `"❌ خطأ — اختار: ..."` — فتظهر النتيجة تلقائياً بلوحة المعلم بدون أي تعديل إضافي هناك.
- بضع أسئلة كانت مفتوحة تمامًا بلا إجابة واحدة صحيحة (رأي شخصي مثل "لو كنت مكان البطل..." أو "أيهما كان أهم في رأيك؟") حُذفت من مصفوفة الأسئلة لأنها لا تصلح لاختيار من متعدد (تبقى الفكرة تُطرح شفهياً من المعلم لو أراد).
- باقي أقسام القطعة (استخرج، أكمل القصة، اكتب خاتمة) بقيت كما هي (كتابة حرة) لأنها ليست أسئلة فهم بإجابة واحدة محددة.

**الاختبار:** فحص برمجي شامل لكل الأسئلة الـ٤٤٨ (كل سؤال له إجابة صحيحة موجودة فعلاً ضمن خيارات المرحلة، صفر أخطاء) + اختبار فعلي بالمتصفح (نقر إجابة صحيحة/خاطئة، التحقق من الألوان والقفل، إغلاق وإعادة فتح المقطع للتأكد من ثبات النتيجة المحفوظة) على عدة مراحل تمثيلية (٢٤، ٢٦، ٣٨، ٤٢) — صفر أخطاء كونسول.

## تحديث 2026-09-10 (٢): واجبات إلكترونية — رابط لكل طالب + تأكيد حل فعلي

طلب محمد: "يكون فيه رابط واجب أرسله للطلاب، الطالب يدخل يكتب اسمه ويحله، وأنا أعرف إنه دخل وحل فعلاً" — بُني نظام واجبات كامل:

- **`Fakker.Homework`** (`shared/progress.js`): وحدة جديدة تحفظ على Firebase — `create(stageId, title, cb)` يُنشئ واجب بمعرّف عشوائي، `recordCompletion(hwId, extra)` يسجّل إنجاز الطالب (اسمه + صورته + نجومه + وقت الإنجاز)، `fetchOne`/`fetchAll` للقراءة.
- **`teacher.html`**: تبويب جديد **"📮 الواجبات"** — المعلم يختار أي مرحلة من الـ٤٢ من قائمة منسدلة، يضغط "إنشاء رابط الواجب"، يطلع له رابط جاهز للنسخ (`index.html?hw=...`). تحته قائمة كل الواجبات السابقة، كل واحد قابل للتوسيع يبيّن أسماء الطلاب اللي حلّوه + نجومهم + وقت الحل.
- **إصلاح جانبي مهم:** `teacher.html` كانت ناقصة سكربتات Firebase (firebase-app-compat.js وfirebase-database-compat.js وfirebase-config.js) من الأساس — يعني حتى لو محمد يفعّل Firebase، تبويب "إجابات الطلاب" ما كان بيشتغل أبداً لأنه ما يقدر يتصل. تم إضافتها الآن (تصلح التبويبين معاً).
- **`index.html`**: يستقبل ويمرر `?hw=...` عبر شاشة الترحيب (اسم فقط، بدون خطوات إضافية) إلى `games/asas.html?hw=...`.
- **`games/asas.html`**: لو دخل الطالب برابط واجب، يفتح له مباشرة المرحلة المحددة (متجاوزاً خريطة المراحل وأي قفل تسلسلي) مع شريط علوي "📮 واجبك من معلمك: ...". عند إنهاء المرحلة (أي نوع: قِطعة/تدريب كلمات/ترتيب جملة أو قصة) يُسجَّل الإنجاز تلقائياً على Firebase بربطه بمعرّف الواجب.
- **⚠️ هذه الميزة تحتاج Firebase حقيقي لتعمل فعلياً** (أكثر من أي ميزة سابقة، لأنها أصلاً مبنية على أن المعلم يشوف من جهازه شيء صار على جهاز الطالب) — بدون Firebase، تبويب "الواجبات" يعرض رسالة واضحة إنه معطّل. الكود جاهز ١٠٠٪ ومُختبر بالكامل بقاعدة بيانات وهمية تحاكي Firebase (محاكاة كاملة: إنشاء واجب، فتح الطالب له، حله، ظهور النتيجة عند المعلم) — بمجرد ما يرسل محمد قيم Firebase الحقيقية، يشتغل فوراً بلا أي تعديل إضافي.

## ميزة جديدة 2026-09-19: مهارات التفكير (بطاقة ثالثة برئيسية فصحون)

طلب محمد: قسم جديد يقيس مهارات التفكير عبر قصص وأسئلة، ويعرض بعد كل قصة "درجة كل مهارة وآلية تحسينها" — أرسل نموذج PDF ("فضل الصداقة" بـ١١ نوع سؤال) وطلب تحويلها اختيار من متعدد قدر الإمكان + ١٤ نسخة إضافية من تأليفنا.

- **الملفات:** `shared/thinking-data.js` (بيانات) + `games/thinking.html` (المحرك، مستقل تماماً عن asas.html) + بطاقة ثالثة بـ`index.html` + تسجيل بـ`FAKKER_GAMES` في `progress.js`
- **٧ مهارات تفكير** (تذكر وفهم، سبب ونتيجة، استنتاج وتفسير، ثروة لغوية، مقارنة وتحليل، استخراج أدلة، تفكير ناقد ورأي) — كل قصة فيها ١٤ سؤال اختيار من متعدد (سؤالين لكل مهارة). أنواع النموذج الأصلي (صل السبب بالنتيجة، قارن الشخصيات، استخرج من النص...) حُوّلت كلها MCQ بذكاء بدل شكل ورقي حر، ما عدا الأسئلة الرأي/الإبداع المفتوحة تماماً اللي حافظنا على روحها بصياغة "أفضل إجابة معلَّلة"
- **١٥ قصة أصلية** (فضل الصداقة من محمد + ١٤ قصة/حكمة من تأليفنا: أمانة، صبر، تعاون، حكمة الكبار، بيئة، شكر، شجاعة، إبداع، مسؤولية، عفو، كرم، تخطيط، ثقة بالنفس، مثابرة) بمستوى رابع-سادس تقريباً
- **بطاقة نتيجة بعد كل قصة:** نجوم + درجة كل مهارة (X/2) + شريط ملوّن (أخضر/برتقالي/أحمر) + نصيحة تحسين نصية لكل مهارة حسب مستوى الأداء، محفوظة أيضاً بشكل تراكمي (زر "ملخص مهاراتي الكلي" بخريطة القصص يجمع كل القصص المحلولة)
- مربوطة بنظام النقاط/المحفظة والنجوم الموجود بالضبط زي أساس القراءة (Fakker.Economy.awardCorrect لكل إجابة صح + awardCompletion عند إنهاء القصة)
- **اختُبرت بالكامل بمحاكاة متصفح حقيقية** (سيرفر بايثون محلي + Claude in Chrome): فتح القصة، جاوبت ١٤ سؤال (صح وخطأ)، تأكدت من تلوين الاختيارات، بطاقة النتيجة ظهرت صحيحة (١٣/١٤، ٣ نجوم، درجة كل مهارة والنصيحة المناسبة)، النجوم انحفظت بخريطة القصص، الملخص الكلي اشتغل، المحفظة زادت صح (٩٢٨→١١٠٨) — صفر أخطاء كونسول من كودنا. **باگ اكتُشف وأُصلح أثناء الاختبار:** عناوين المهارات (🧠 التذكر والفهم...) كانت تُبنى بالكود لكن ما تُلحق بالـDOM فعلياً (bug: `wrap.appendChild(row)` بدل `sec.appendChild(row)`) — ما كان يبان إلا بتشغيل فعلي بالمتصفح، مو بمراجعة الكود.
- 🔲 لسه ما وُصلت بتبويب لوحة المعلم (teacher.html) — البيانات محفوظة بـlocalStorage جاهزة للتوسعة لاحقاً لو احتاج محمد تقرير مهارات على مستوى الفصل

## تحديث 2026-09-19 (٢): ربط مهارات التفكير بنظام الواجبات + إصلاح باگ فقدان بيانات حقيقي

طلب محمد ربط "مهارات التفكير" بنفس نظام الواجبات الإلكترونية (رابط واتساب → الطالب يكتب اسمه ويحل → يظهر له درجته وأخطاؤه → يظهر للمعلم من حل والحلول).

- **`Fakker.Homework.create` صار يأخذ `gameId`** (`"asas"` أو `"thinking"`) ويحفظه مع الواجب، عشان `index.html` يعرف يوجّه الطالب لصفحة اللعبة الصحيحة (`games/asas.html?hw=...` أو `games/thinking.html?hw=...`) بدل التوجيه الثابت لأساس القراءة دايماً.
- **`teacher.html`**: قائمة إنشاء الواجب صارت مقسومة بـ`optgroup` (🌱 أساس القراءة / 🧩 مهارات التفكير)، وقائمة "إجابات الطلاب" تعرف تفرّق بين النوعين (معرّف مرحلة مهارات التفكير مسبوق بـ"t" زي "t3" عشان ما يتصادم مع أرقام مراحل أساس القراءة 1-42).
- **`games/thinking.html`**: صار يدعم `?hw=...` بنفس منطق asas.html (بانر "📮 واجبك من معلمك"، فتح القصة المحددة مباشرة بدون خريطة)، ويحفظ كل إجابة MCQ فورياً بصيغة "✅ صحيح — اختار: ..." عشان تظهر عند المعلم، وبطاقة النتيجة صار فيها قسم "🧐 راجع أخطاءك" يسرد كل سؤال أخطأ فيه الطالب مع إجابته والإجابة الصحيحة.
- **🐛 باگ حقيقي خطير اكتُشف وأُصلح:** `Fakker.Progress._save()` كانت تستخدم `.set()` على `students/{id}` بالكامل (بس `{name, avatar, updatedAt, progress}`) — أي حفظ تقدّم جديد (زي إنهاء أي مرحلة) كان **يمسح بصمت** أي بيانات إجابات محفوظة سابقاً تحت نفس الطالب (`students/{id}/answers/...`) لأن `.set()` يستبدل كل شيء تحت المسار لا يدمجه. هذا كان يكسر ميزة "إجابات الطلاب" بالكامل لأساس القراءة ومهارات التفكير معاً في أي استخدام حقيقي. **الإصلاح:** تغييرها لـ`.update()` (دمج بدل استبدال) — سطر واحد، بدون أي أثر جانبي على بقية النظام.
- **اختُبر الإصلاح بالتحديد على أسوأ سيناريو توقيت** (إجابة كل الأسئلة ثم الضغط على "إنهاء" فوراً بنفس اللحظة، عشان نتأكد إن حفظ التقدّم ما يمسح حفظ الإجابات المتأخر) — بقاعدة بيانات وهمية تحاكي Firebase API (مؤقتة، أُزيلت بعد الاختبار عبر `git checkout`) على منفذ محلي مختلف لتفادي كاش كروم: أنشأ "معلم" واجب مهارات تفكير → فتحه "طالب" من الرابط → أجاب ١٤ سؤال (٢ متعمّدين خطأ) → البطاقة عرضت ١٢/١٤ وقسم "راجع أخطاءك" صحيح → المعلم شاف تبويب الواجبات (١٢/١٤، ★★★) وتبويب إجابات الطلاب (١٤/١٤ إجابة، كل سؤال بنصه الكامل + ✅/❌) — صفر فقدان بيانات بعد الإصلاح (كان الفقدان ١٠٠٪ قبله).

## تحديث 2026-09-19 (٣): باگ حرج بمهارات التفكير + زيادة ٥ قطع لكل قسم بالتطبيق

**🐛 باگ حرج اكتشفه محمد بنفسه بعد الاستخدام الفعلي:** كل الـ٢٨٠ سؤال بمهارات التفكير (٢٠ قصة × ١٤ سؤال) كانت الإجابة الصحيحة فيها بموضع الخيار الأول ثابتاً (`correct:0`) — نسيت أخلط ترتيب الخيارات وقت البناء الأول. أي طالب ملاحظ يكتشف النمط ويجاوب صح بلا قراءة، يُبطل الهدف من القياس كله. **الإصلاح:** سكربت Node خلط (Fisher–Yates) لكل سؤال مع تحديث فهرس `correct` تبعاً لمكان الإجابة الجديد، تحقق برمجي من توزيع متساوٍ تقريباً على المواضع الأربعة (كل موضع ~٢٥٪) + اختبار بصري فعلي بالمتصفح يتأكد إن التلوين صح/خطأ يتبع الموضع الجديد صح. **درس مهم:** هذا النوع من الأخطاء (بيانات ثابتة الشكل لكنها منطقياً معطوبة) ما يظهر بفحص الكود العادي ولا بعد التحقق الآلي من بنية البيانات — يحتاج عين بشرية فعلية تلعب اللعبة، بالضبط زي ما لاحظ محمد.

طلب محمد أيضاً: "زد خمس قطع" لكل قسم فيه قطع/قصص بالتطبيق كله (وضّحنا معه إنها تشمل ٥ مجموعات بأساس القراءة + مهارات التفكير = ٣٠ قطعة جديدة إجمالاً):

- **مهارات التفكير:** ١٥ → **٢٠ قصة** (قصص ١٦-٢٠: الفضول العلمي، روح الفريق، المحطة الغريبة، آخر لحظة، ليلة انقطاع الكهرباء) — نفس نظام ١٤ سؤال/٧ مهارات، مُختبرة بالخلط الصحيح من البداية
- **أساس القراءة (٤ مجموعات + مستويات القطع المتدرجة):**
  - قِطع قراءة علاجية (مرحلة ٢٤): ١٥ → **٢٠**
  - قصص فهم واستيعاب (مرحلة ٢٥): ١٥ → **٢٠**
  - استيعاب مقروء ٢ (مرحلة ٢٦): ٣٠ → **٣٥**
  - استيعاب مقروء ٣ (مرحلة ٢٧): ١٥ → **٢٠**
  - قِطع متدرجة، المستويات ١-٥ (مراحل ٣٨-٤٢): قطعة واحدة جديدة لكل مستوى (٣ → **٤** لكل مستوى)، بنفس تدرج الصعوبة والـschema الخاص بكل مستوى بالضبط
- **اختُبرت كل الإضافات برمجياً** (node --check لكل ملف + تحقق تطابق عدد الأسئلة مع عدد الإجابات لكل قطعة جديدة عبر تحميل حقيقي لـASAS_STAGES) **وبمحاكاة متصفح فعلية**: فُتحت مرحلة ٣٨ كاملة ودار عليها المحرك حتى وصل للقطعة الجديدة الرابعة وعرضها صح مع مشتتات MCQ مسحوبة تلقائياً من القطع الأخرى (تأكيد إن القطع الجديدة والقديمة تتشارك تجمّع المشتتات بشكل صحيح)، وفُتحت مرحلة ٢٤ وتأكدنا إن القطع الجديدة تظهر كمصدر مشتتات للقطع القديمة أيضاً.

## تحديث 2026-09-21: زيادة "رتّب الكلمات لجملة" و"رتّب الجمل لقصة" + ٥ قصص إضافية لمهارات التفكير

محمد لاحظ إننا زدنا قطع أساس القراءة (٢٤-٢٧) ومهارات التفكير بالجولة اللي قبل، لكن نسينا مرحلتي "رتّب الكلمات لجملة" (٢٢) و"رتّب الجمل لقصة" (٢٣) رغم إنه طلب زيادتهم أيضاً بنفس القاعدة (+٥):

- **رتّب الكلمات لجملة (مرحلة ٢٢):** ٣٠ → **٣٥** جملة مبعثرة، تشكيل كامل بنفس أسلوب الموجود
- **رتّب الجمل لقصة (مرحلة ٢٣):** ٣٠ → **٣٥** قصة (٤ جمل لكل واحدة)، تشكيل كامل
- **مهارات التفكير:** ٢٠ → **٢٥ قصة** (قصص ٢١-٢٥: مكافأة لم تُستحق [صدق مقابل غش]، الاختلاف الجميل [احترام التنوع]، الخبر غير المؤكد [تفكير نقدي ضد الإشاعات]، الاعتذار الحقيقي [إصلاح الخطأ بفعل لا كلام]، يد تمتد بلا مقابل [تطوع ومساعدة المسنين]) — نفس نظام ١٤ سؤال/٧ مهارات
- **مهم:** أعدنا خلط ترتيب خيارات الإجابة الصحيحة (Fisher–Yates) على الملف كاملاً (٢٥ قصة × ١٤ سؤال = ٣٥٠ سؤال) مرة ثانية بعد الإضافة، عشان القصص الجديدة ما تتكرر فيها نفس باگ "الإجابة دايماً أول خيار" اللي صار قبل — تحقّق التوزيع: كل موضع تقريباً ٢٥٪ من ٣٥٠
- **اختُبرت الإضافات فعلياً بمحاكاة متصفح** عبر حقن JS مباشر (لأن أدوات لقطة الشاشة تعطلت مؤقتاً بجهاز محمد أثناء الجلسة): قصة جديدة كاملة بمهارات التفكير (مكافأة لم تُستحق) حُلّت ١٤/١٤ وطلعت البطاقة صح، وقصة جديدة بـ"رتّب الجمل لقصة" (الرسمة الفائزة) رُتّبت صح ورفع النقاط تلقائياً — صفر أخطاء كونسول.
