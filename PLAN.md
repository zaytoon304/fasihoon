# فصحون — تطبيق مستقل لتعلّم اللغة العربية

مأخوذ من مشروع فكّر بتاريخ 2026-09-08 ليصير تطبيقاً مستقلاً بذاته، بدون حذف أي شيء من نسخة فكّر الأصلية.

## خطوات البناء
- [x] إنشاء المجلد + نسخ الملفات ذات الصلة من فكّر: `games/fasihoon.html`، `games/asas.html`، وكل ملفات `shared/` المطلوبة (style.css، firebase-config.js، economy-config.js، economy.js، asas-data*.js)، و`store.html`، `teacher.html`
- [x] `shared/progress.js` أُعيد كتابته: `FAKKER_GAMES` صار نشاطين فقط (فصحون + أساس القراءة)، أُزيل `Fakker.Daily` و`Fakker.CheckIns` بالكامل (غير مستخدمين بلا بوابة حصة انتظار)
- [x] أُزيلت كل استدعاءات `Fakker.CheckIns.isCheckedIn()` من `games/fasihoon.html`، `games/asas.html`، و`store.html` (كانت تكسر الصفحات بصمت — اكتُشف بالاختبار الفعلي بالمتصفح)
- [x] `teacher.html`: أُزيل تبويب "سجل حصص الانتظار" بالكامل (تبويب + دالة renderCheckins + استخدامات Fakker.CheckIns بالإحصائيات)
- [x] إعادة تسمية العلامة التجارية من "فكّر 🧠" إلى "فصحون 📖" بكل الملفات (العناوين، الشريط العلوي)
- [x] `index.html` جديد كلياً: شاشة ترحيب (اسم + شخصية، بلا بوابة حصة انتظار) → صفحة رئيسية ببطاقتين كبيرتين (📖 فصحون يفتح شاشة اختيار المستوى الدراسي الخمسة، 🌱 أساس القراءة يفتح مباشرة لأنها تدير مراحلها بنفسها)
- [x] اختُبر بالمتصفح فعلياً بالكامل: الترحيب، الصفحة الرئيسية، اختيار المستوى، لعب فصحون، أساس القراءة (٢٥ مرحلة)، المتجر، لوحة المعلم (فتح بالـ PIN، تبويب الإحصائيات يعرض النشاطين الصحيحين) — صفر أخطاء كونسول بعد إصلاح مراجع CheckIns المتبقية ✅
- [x] تهيئة git + رفع على GitHub — مستودع عام منفصل: `github.com/zaytoon304/fasihoon`، منشور: **https://zaytoon304.github.io/fasihoon/**

## ملاحظة
نسخة فكّر الأصلية (`../fakker/games/fasihoon.html` و`../fakker/games/asas.html`) لم تُمس إطلاقاً — هذا مشروع مستقل بمجلد وشيفرة خاصة به بالكامل.

## تحديثات ما بعد الإطلاق (نفس اليوم 2026-09-09)

**١. فتح كل المراحل دفعة وحدة (للمعلم):** `games/asas.html?unlock=all` يفتح كل مراحل أساس القراءة بـ٣ نجوم فوراً على أي جهاز يُفتح فيه — يفيد وقت الحصة بدل انتظار التقدم التسلسلي الطبيعي.

**٢. مساحة إجابة مكتوبة بكل أسئلة قِطع القراءة** (بدل نقاش شفهي فقط) — تُحفظ محلياً تلقائياً، وأيضاً على Firebase لو مفعّل.

**٣. مرحلتان جديدتان من قِطع الاستيعاب المرسلة من محمد** (٢٦: ٣٠ قطعة، ٢٧: ١٥ قطعة أطول وأكثر ثراءً بالأحداث) — كل قطعة: قصة + أسئلة فهم + إملاء + استخرج + أكمل القصة + خاتمة.

**٤. تتبع إجابات الطلاب للمعلم:** `Fakker.Answers` يحفظ كل إجابة على Firebase (اسم الطالب + النص)، وتبويب جديد بلوحة المعلم **"✍️ إجابات الطلاب"** يسرد كل طالب وعدد إجاباته، وبالضغط عليه تظهر كل إجاباته مجمّعة حسب المقطع مع نص السؤال. **يحتاج مشروع Firebase حقيقي مفعّل ليعمل عبر الأجهزة** (بدونه، كل جهاز يحفظ بياناته محلياً فقط ولوحة المعلم تعرض تنبيهاً واضحاً بدل خطأ) — بانتظار محمد يرسل قيم Firebase config.

**٥. الكلمات عالية التكرار (Sight Words) — ١٠ مراحل جديدة (٢٨-٣٧):** ضمائر، كلمات مكان، كلمات زمن، كلمات ربط، أدوات سؤال، أفعال متكررة (مرحلتان)، أسماء قصص متكررة (مرحلتان)، صفات متكررة — كلمات يجب أن تُقرأ مباشرة بلا تهجئة، مبنية من قائمة تربوية فصّلها محمد بالكامل.

**٦. قِطع متدرجة حقيقياً — ٥ مراحل جديدة (٣٨-٤٢)، ١٥ قطعة أصلية من تأليف جديد:** المستوى ١ (جمل ٤-٦ كلمات فقط، بلا إملاء/استخراج) ← المستوى ٢ (ربط بـثم/بعد/عند/لأن) ← المستوى ٣ (سبب ونتيجة + حوار بسيط) ← المستوى ٤ (قصة ٦-٨ جمل + استنتاج) ← المستوى ٥ (سرد كامل + ترتيب أحداث + استنتاج + خاتمة). الكلمات الذهبية الخمسون تتكرر عمداً عبر القِطع لتثبيت القراءة الفورية.

**النطاق الكامل الآن: ٤٢ مرحلة بأساس القراءة.** كل التحديثات اختُبرت بالمتصفح فعلياً (إكمال كامل لكل مرحلة جديدة بلا أخطاء كونسول) قبل الرفع. لا عمل ناقص معروف غير خطوة Firebase (بانتظار محمد).

## تحديث 2026-09-10: أسئلة الفهم صارت اختيار من متعدد

بناءً على طلب محمد: "بدلاً من أن يكتب الطالب، تعطيه اختيار الإجابة من متعدد" — كل أسئلة الفهم (comprehension) بجميع مراحل قِطع القراءة (٢٤، ٢٥، ٢٦، ٢٧، ٣٨-٤٢ = ٩ مراحل، ٤٤٨ سؤال إجمالاً) تحولت من صندوق كتابة حر إلى أزرار اختيار من متعدد حقيقية.

**كيف تعمل تقنياً:**
- كل سؤال بملفات `asas-data-phase4/5/6/7.js` صار له `answers: [...]` — مصفوفة موازية لـ`comprehension`/`questions`، فيها الإجابة الصحيحة النصية لكل سؤال بنفس الترتيب.
- محرك العرض (`games/asas.html`) يبني ٤ اختيارات لكل سؤال: الإجابة الصحيحة + ٣ مشتتات تُسحب عشوائياً من إجابات باقي مقاطع نفس المرحلة (لا تكرار، ولا تُستخدم إجابة السؤال نفسه كمشتت).
- عند اختيار الطالب: كل الأزرار تُقفل فوراً (`locked`)، الإجابة الصحيحة تلوّن أخضر دائماً، واختيار الطالب الخاطئ (إن وُجد) يلوّن أحمر. يُحفظ الاختيار في localStorage وعلى Firebase (لو مفعّل) بنص مثل: `"✅ صحيح — اختار: ..."` أو `"❌ خطأ — اختار: ..."` — فتظهر النتيجة تلقائياً بلوحة المعلم بدون أي تعديل إضافي هناك.
- بضع أسئلة كانت مفتوحة تمامًا بلا إجابة واحدة صحيحة (رأي شخصي مثل "لو كنت مكان البطل..." أو "أيهما كان أهم في رأيك؟") حُذفت من مصفوفة الأسئلة لأنها لا تصلح لاختيار من متعدد (تبقى الفكرة تُطرح شفهياً من المعلم لو أراد).
- باقي أقسام القطعة (استخرج، أكمل القصة، اكتب خاتمة) بقيت كما هي (كتابة حرة) لأنها ليست أسئلة فهم بإجابة واحدة محددة.

**الاختبار:** فحص برمجي شامل لكل الأسئلة الـ٤٤٨ (كل سؤال له إجابة صحيحة موجودة فعلاً ضمن خيارات المرحلة، صفر أخطاء) + اختبار فعلي بالمتصفح (نقر إجابة صحيحة/خاطئة، التحقق من الألوان والقفل، إغلاق وإعادة فتح المقطع للتأكد من ثبات النتيجة المحفوظة) على عدة مراحل تمثيلية (٢٤، ٢٦، ٣٨، ٤٢) — صفر أخطاء كونسول.

## تحديث 2026-09-10 (٢): واجبات إلكترونية — رابط لكل طالب + تأكيد حل فعلي

طلب محمد: "يكون فيه رابط واجب أرسله للطلاب، الطالب يدخل يكتب اسمه ويحله، وأنا أعرف إنه دخل وحل فعلاً" — بُني نظام واجبات كامل:

- **`Fakker.Homework`** (`shared/progress.js`): وحدة جديدة تحفظ على Firebase — `create(stageId, title, cb)` يُنشئ واجب بمعرّف عشوائي، `recordCompletion(hwId, extra)` يسجّل إنجاز الطالب (اسمه + صورته + نجومه + وقت الإنجاز)، `fetchOne`/`fetchAll` للقراءة.
- **`teacher.html`**: تبويب جديد **"📮 الواجبات"** — المعلم يختار أي مرحلة من الـ٤٢ من قائمة منسدلة، يضغط "إنشاء رابط الواجب"، يطلع له رابط جاهز للنسخ (`index.html?hw=...`). تحته قائمة كل الواجبات السابقة، كل واحد قابل للتوسيع يبيّن أسماء الطلاب اللي حلّوه + نجومهم + وقت الحل.
- **إصلاح جانبي مهم:** `teacher.html` كانت ناقصة سكربتات Firebase (firebase-app-compat.js وfirebase-database-compat.js وfirebase-config.js) من الأساس — يعني حتى لو محمد يفعّل Firebase، تبويب "إجابات الطلاب" ما كان بيشتغل أبداً لأنه ما يقدر يتصل. تم إضافتها الآن (تصلح التبويبين معاً).
- **`index.html`**: يستقبل ويمرر `?hw=...` عبر شاشة الترحيب (اسم فقط، بدون خطوات إضافية) إلى `games/asas.html?hw=...`.
- **`games/asas.html`**: لو دخل الطالب برابط واجب، يفتح له مباشرة المرحلة المحددة (متجاوزاً خريطة المراحل وأي قفل تسلسلي) مع شريط علوي "📮 واجبك من معلمك: ...". عند إنهاء المرحلة (أي نوع: قِطعة/تدريب كلمات/ترتيب جملة أو قصة) يُسجَّل الإنجاز تلقائياً على Firebase بربطه بمعرّف الواجب.
- **⚠️ هذه الميزة تحتاج Firebase حقيقي لتعمل فعلياً** (أكثر من أي ميزة سابقة، لأنها أصلاً مبنية على أن المعلم يشوف من جهازه شيء صار على جهاز الطالب) — بدون Firebase، تبويب "الواجبات" يعرض رسالة واضحة إنه معطّل. الكود جاهز ١٠٠٪ ومُختبر بالكامل بقاعدة بيانات وهمية تحاكي Firebase (محاكاة كاملة: إنشاء واجب، فتح الطالب له، حله، ظهور النتيجة عند المعلم) — بمجرد ما يرسل محمد قيم Firebase الحقيقية، يشتغل فوراً بلا أي تعديل إضافي.
