# فصحون — ذاكرة المشروع

تطبيق مستقل لتعلّم اللغة العربية (قراءة وكتابة)، مأخوذ من مشروع فكّر (`../fakker/`) بتاريخ 2026-09-08 بناءً على طلب محمد: "احتاج فصحون تطبيق مستقل لكن لا تحذف من هو في فكّر". **النسخة الأصلية داخل فكّر باقية بدون أي تعديل** — هذا مشروع منفصل تماماً بمجلده الخاص.

## المحتوى
- **فصحون** (`games/fasihoon.html`) — ٥ مستويات دراسية (أول ابتدائي / ثاني-ثالث / رابع-خامس-سادس / متوسط / ثانوي)، لعبة اختبار لغوي (حروف/كلمات/جمل/قدرات لغوية)
- **أساس القراءة** (`games/asas.html`) — ٤٢ مرحلة، تعليم القراءة من الصفر (حرف ← حرفين ← كلمة ← مدود/تنوين/شدة/لام/تاء ← ترتيب جملة/قصة ← قِطع قراءة بفهم وإملاء ← Sight Words ← قِطع متدرجة ٥ مستويات)
- **مهارات التفكير** (`games/thinking.html` + `shared/thinking-data.js`) — ١٥ قصة أصلية، ١٤ سؤال اختيار من متعدد لكل قصة يقيسون ٧ مهارات تفكير (تذكر/سبب ونتيجة/استنتاج/مفردات/مقارنة/أدلة/رأي ناقد)، بطاقة نتيجة بعد كل قصة بدرجة كل مهارة ونصيحة تحسين + ملخص تراكمي. مستقلة كلياً عن asas.html
- نظام نقاط/محفظة كامل (مشترك بين الأنشطة الثلاثة) + متجر مكافآت (`store.html`) + لوحة معلم (`teacher.html`، PIN افتراضي 2026)

## قرارات مهمة (فروقات عن نسخة فكّر)
- **بلا بوابة "حصة انتظار"** — كانت خاصة بسيناريو لوحة ذكية واحدة تتناوب عليها فصول متعددة طول اليوم بفكّر؛ فصحون المستقلة تُفتح مباشرة بالاسم والصورة بدون تسجيل فصل/حصة
- `shared/progress.js` معدّل: `FAKKER_GAMES` يحوي نشاطين فقط (فصحون + أساس القراءة) بدل ١٢ لعبة فكّر؛ `Fakker.Daily` (تحدي اليوم) و`Fakker.CheckIns` أُزيلا بالكامل (غير مستخدمين)
- `teacher.html`: تبويب "سجل حصص الانتظار" أُزيل لعدم الحاجة له
- باقي الأنظمة (Wallet, Economy, Rewards, Redemptions, TeacherAuth, Settings, FX) منسوخة كما هي بلا تغيير
- HTML/CSS/JS عادي بدون أدوات بناء (نفس أسلوب فكّر)
- Firebase Realtime Database اختياري لحفظ التقدم (يعمل محلياً بـ localStorage بدون مفاتيح حقيقية)

## كيف تشغّل المشروع
افتح `index.html` مباشرة بالمتصفح (لا حاجة لسيرفر أو npm).

## هيكل الملفات
```
fasihoon/
├── index.html          ← الصفحة الرئيسية (بطاقتان: فصحون / أساس القراءة)
├── store.html
├── teacher.html
├── PLAN.md / CLAUDE.md
├── games/
│   ├── fasihoon.html
│   ├── asas.html
│   └── thinking.html (مهارات التفكير)
├── shared/
│   ├── style.css, firebase-config.js
│   ├── progress.js (معدّل عن فكّر)
│   ├── economy-config.js, economy.js
│   ├── asas-data.js + phase2..7.js (محتوى أساس القراءة)
│   └── thinking-data.js (١٥ قصة مهارات التفكير)
└── content-source/      ← مرجع القصص المتقدمة (مصدر بيانات phase4)
```
